# Additional dependencies required for running the "stable" version of directlfq GUI.
# Only usage of fixed versions is allowed, and all dependencies listed here must also be
# included in `requirements_gui_loose.txt` (enforced by a test).
panel==0.10.3
dash==2.5.1
matplotlib==3.5.3